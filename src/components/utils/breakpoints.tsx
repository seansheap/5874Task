const size = {
    tn:'20px',
    xs:'320px',
    sm:'768px',
    lg: '1200px',
   }
   const device = {
    tn: `(min-width: ${size.tn})`,
    xs: `(min-width: ${size.xs})`,
    sm: `(min-width: ${size.sm})`,
    lg: `(min-width: ${size.lg})`
   }
export {size, device}